node_modules
.next
.next.*
out
npm-debug.log*
.DS_Store

# Environment / secrets — nothing holding a key may ever be committed.
.env
.env.*
!.env.example
server/.env
.env.vercel*
server/.env.vercel*

# Runtime data
server/data/*.sqlite
server/data/*.sqlite-*
server/uploads/*
!server/uploads/.gitkeep
tsconfig.tsbuildinfo
.vercel
.workbuddy-ai/

# Local-only scratch notes + AI tool config (opencode.jsonc holds an embedded key — never commit)
FIX_APPLIED.md
SOLUTION_SUMMARY.md
opencode.jsonc
